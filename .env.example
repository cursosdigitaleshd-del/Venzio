# ──────────────────────────────────────────
# Venzio – Voice Sales Agent SaaS
# Copia este archivo a .env y completa los valores
# ──────────────────────────────────────────

# OpenAI (configurar en EasyPanel para producción)
OPENAI_API_KEY=tu-clave-openai-aqui

# Servicios internos (usar nombres de contenedor en Docker)
STT_SERVICE_URL=http://stt-service:8001
TTS_SERVICE_URL=http://tts-service:8002

# Base de datos
DATABASE_URL=sqlite:///./venzio.db

# Autenticación JWT
SECRET_KEY=cambia-esto-por-una-clave-segura-de-32-chars
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Control de concurrencia
MAX_GLOBAL_SESSIONS=10

# STT Config
WHISPER_MODEL=base
WHISPER_LANGUAGE=es

# TTS Config – voz por defecto al inicializar DB
DEFAULT_VOICE_NAME=Español Davefx
DEFAULT_VOICE_FILE=es_ES-davefx-medium.onnx

# OpenAI LLM Config
LLM_MODEL=gpt-4o-mini
LLM_MAX_TOKENS=500
LLM_TEMPERATURE=0.7
LLM_TIMEOUT=30

# Admin
ADMIN_EMAIL=admin@venzio.com
ADMIN_PASSWORD=Admin1234!

# CORS – orígenes permitidos separados por coma
ALLOWED_ORIGINS=http://localhost,http://localhost:3000

# Email settings (configurar en EasyPanel para producción)
SMTP_SERVER=tu-servidor-smtp.com
SMTP_PORT=587
SMTP_USERNAME=tu-email@dominio.com
SMTP_PASSWORD=tu-contraseña-smtp
CONTACT_EMAIL=destino@dominio.com
